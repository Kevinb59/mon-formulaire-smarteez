{
  "name": "smart-z",
  "version": "1.0.0",
  "description": "Coques personnalisées",
  "main": "index.js",
  "scripts": {
    "build": "echo \"No build step required\""
  },
  "dependencies": {
    "stripe": "^10.0.0",
    "express": "^4.17.1",
    "cors": "^2.8.5"  // Ajout de la dépendance 'cors'
  },
  "engines": {
    "node": "18"  // Spécifie la version de Node.js pour Vercel
  }
}
